const routesPath: Routes = {
  main: '/',
  works: '/works',
  price: '/price',
  error: '/error',
  contacts: '#contact',
};
export default routesPath;

type Routes = {
  main: string;
  works: string;
  price: string;
  error: string;
  contacts: string;
};
