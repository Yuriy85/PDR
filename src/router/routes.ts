const routesPath: Routes = {
  main: '/',
  works: '/works',
  price: '/price',
  error: '/error',
  contacts: '/contacts',
};
export default routesPath;

type Routes = {
  main: string;
  works: string;
  price: string;
  error: string;
  contacts: string;
};
